package testingil.cleancode.arch;

public class DistanceProvider {

    public double getDistance(Location startingPoint, Location endPoint) {
        // Call outside the system
        return 0;
    }

}
